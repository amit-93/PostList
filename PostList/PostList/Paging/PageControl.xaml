<UserControl x:Class="PostList.Paging.PageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel  Orientation="Horizontal"  HorizontalAlignment="Center" Grid.Column="0" Grid.Row="1"  >
            <Button Margin="4,0"  Content="First"  Command="{Binding PageCommand}" CommandParameter="First" />
            <Button Margin="4,0"  Content="Previous"  Command="{Binding PageCommand}" CommandParameter="Previous"/>
            <StackPanel   VerticalAlignment="Center"  Orientation="Horizontal">
                <TextBlock  Text="{Binding Start}"/>
                <TextBlock  Text=" to "/>
                <TextBlock  Text="{Binding End}"/>
                <TextBlock  Text=" of "/>
                <TextBlock  Text="{Binding TotalItems}"/>
            </StackPanel>
            <Button Margin="4,0" Content="Next" Command="{Binding PageCommand}" CommandParameter="Next"/>
            <Button Margin="4,0" Content="Last" Command="{Binding PageCommand}" CommandParameter="Last"/>
        </StackPanel>
</UserControl>
